import styles from './UserProfile.module.css';

const UserProfile = () => {
  return (
    <main className={styles.profile}>
      <h2>User Profile</h2>
    </main>
  );
};

export default UserProfile;
